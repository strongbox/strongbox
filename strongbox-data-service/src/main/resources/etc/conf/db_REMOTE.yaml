strongbox:
    db:
      janusgraph:
        # Note: Configuring JG with a remote Cassandra instance would require you to set the `storage.hostname` property in JanusGraph.
        config-location: file:${strongbox.home}/etc/conf/janusgraph-cassandra.properties
